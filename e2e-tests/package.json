{
  "name": "e2e-tests",
  "version": "1.0.0",
  "description": "E2E tests for AI Workflow Automation Dashboard",
  "scripts": {
    "test": "playwright test",
    "test:list": "playwright test --list"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.0.0"
  }
}
